{
  "dependencies": {
    "three": "^0.149.0"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.8.3",
    "parcel": "^2.8.3"
  },
  "scripts": {
    "parcel": "parcel",
    "start": "npm run parcel -- index.html",
    "build": "rm -rf .cache dist && parcel build ./index.html --public-url ./"
  }
}
